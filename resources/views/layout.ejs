<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Real-time Pizza App</title>
    <link rel="icon" href="favicon.ico" type="-icon" />
    <link rel="preconnect" href="https://fonts.gstatic.com">
    <link href="https://fonts.googleapis.com/css2?family=Lato:wght@300;400;700&display=swap" rel="stylesheet">
    <link href="https://unpkg.com/tailwindcss@^2/dist/tailwind.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/app.css">
</head>
<body>
   <nav class="container  mx-auto flex items-center justify-between py-4"><!--tailwind css container ,pushing things on right side-->
    <div>
        <img src="/img/logo.png" alt="logo">
    </div>
     <div>
         <ul class="flex items-center">
             <li class="ml-20"><a href="/">Menu</a></li>
             <li class="ml-20" ><a href="#">Offers</a></li>
             <% if(user)  {%>
                <!--to use post method in link we need form-->
                <li class="ml-20">
                    <form id="logout" action="/logout" method="POST">
                        <a onclick="document.getElementById('logout').submit()" href="#">Logout</a>
                    </form>
                    </li>
            
            <% }else{%>
                <li class="ml-20"><a href="/register">Register</a></li>
                <li class="ml-20"><a href="/login">Login</a></li>
                <% } %>
             
             <li class="ml-20"><a href="/cart" class="inline-block px-4 py-2 rounded-full flex items-center">
                <span id="cartCounter" class="text-white font-bold"><%= session.cart ? session.cart.totalQty : '' %></span> <!-- session has to be defined in global middleware or something so that it doesn't throw error     also in beginning of session  we have to check if the session exist then shiw the no. or else show the empty string-->
                
                <img src="/img/cart.png" alt=""></a></li>
         </ul>
     </div>


   </nav>
   <%- body %>
   <footer></footer>
    <script src="/js/app.js"></script>
    
</body>
</html>